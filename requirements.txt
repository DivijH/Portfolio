# Core
# pip picks 6.0 on Python >=3.12 and 5.2 LTS on older interpreters (Requires-Python).
Django>=5.2,<6.1
Pillow>=10.0        # image uploads (avatar, publication figures) + OG image
whitenoise>=6.6     # static file serving (used in production; harmless in dev)

# Production (WSGI server for deployment)
gunicorn>=22.0
